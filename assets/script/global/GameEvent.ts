const enum EventId {
    tipsViewShow = "tipsViewShow",
    hideBanner = "hideBanner",
    showWxVideoAd = "showWxVideoAd",
    showBanner = "showBanner",
    loadRes = "loadRes",
    loadDir = "loadDir",
    switchMainMenuLayer = "switchMainMenuLayer",
    
}
enum ViewId {
    tipsViewShow = "tipsViewShow",
}

/** 地图相关事件 */
enum MapId {
    Build = "Build",
}

enum EditId {
    Confirm = "Confirm",
    Cancel = "Cancel",
    Bag = "Bag",
    Rotate = "Rotate",
    Select = "Select",
}

enum NetId {
    test = "test"
}

export { EventId, ViewId, MapId, EditId, NetId }
